apiVersion: v1
kind: Service
metadata:
  name: {{ include "cac-utils.fullname" . }}
  labels:
    {{- include "cac-utils.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cac-utils.selectorLabels" . | nindent 4 }}
